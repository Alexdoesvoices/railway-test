{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "RAILPACK",
      "buildCommand": "bun install && bun svelte-kit sync && bun run build"
    },
    "deploy": {
      "startCommand": "bun ./build/index.js"
    }
  }